# Base URL of the contabilidad-os deployment this app talks to.
# Required in production. Falls back to http://localhost:3000 in dev.
#
# Must be listed in contabilidad-os's API_ALLOWED_ORIGINS env var so CORS
# preflight passes.
VITE_API_URL=https://your-contabilidad-os.vercel.app
